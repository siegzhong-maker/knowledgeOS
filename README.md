# KnowledgeOS - 通用知识管理平台

> **项目说明**：一个现代化的知识管理系统，支持文本、URL、PDF 等多种格式的内容采集，并通过 AI 自动提取和管理知识点。支持多知识库构建和智能问答。

## ✨ 主要特性

### 📚 内容采集
- **URL 链接解析**：自动提取网页文本内容
- **文本直接输入**：支持纯文本内容录入
- **PDF 文件上传**：支持 PDF 文件上传和内容提取
- **Memo 灵感笔记**：快速记录想法和笔记

### 🤖 AI 能力
- **智能知识提取**：使用 DeepSeek AI 自动从文档中提取结构化知识点
- **AI 对话功能**：基于知识库的上下文感知对话
- **智能摘要生成**：自动生成文档摘要
- **标签建议**：AI 辅助标签分类

### 📖 知识管理
- **多知识库支持**：支持创建和管理多个知识库，灵活组织知识
- **知识图谱**：可视化展示知识点之间的关联关系
- **时间线视图**：按时间维度查看知识演变
- **标签系统**：灵活的标签管理，支持分类和筛选
- **相关知识推荐**：智能推荐相关知识点

### 🔧 高级功能
- **智能问答工作台**：基于知识库的 AI 智能问答服务
- **上下文管理**：支持多场景上下文切换
- **PDF 查看器**：内置 PDF 阅读器，支持页码跳转
- **数据导出**：支持导出知识数据
- **性能监控**：内置性能监控面板

## 🛠 技术栈

- **前端**：Vanilla JavaScript + HTML/CSS (Tailwind CSS)
- **后端**：Node.js + Express
- **数据库**：SQLite（开发环境）/ PostgreSQL（生产环境）
- **AI 服务**：DeepSeek API
- **部署**：支持 Docker、Railway 等平台

## 🚀 快速开始

### 系统要求

- Node.js >= 20.0.0
- npm >= 10.0.0

### 安装步骤

1. **克隆项目**

```bash
git clone https://github.com/siegzhong-maker/knowledgeOS.git
cd knowledgeOS
```

2. **安装依赖**

```bash
npm install
```

3. **初始化数据库**

```bash
npm run init-db
```

这将创建 SQLite 数据库并初始化表结构。如果使用 PostgreSQL，请先配置 `DATABASE_URL` 环境变量。

4. **启动后端服务**

```bash
npm run dev
```

后端服务将在 `http://localhost:3000` 启动。

5. **打开前端**

**方式一：直接打开（推荐）**

直接在浏览器打开 `frontend/index.html` 文件。

**方式二：使用 HTTP 服务器**

```bash
# 使用 Python
cd frontend
python -m http.server 8080

# 或使用 Node.js http-server
npx http-server frontend -p 8080
```

然后访问 `http://localhost:8080`

6. **配置 API Key**

1. 在应用侧边栏底部点击用户头像或设置图标
2. 输入 DeepSeek API Key（在 [DeepSeek Platform](https://platform.deepseek.com) 获取）
3. 点击"测试连接"验证
4. 保存设置

7. **创建知识库**

系统初始化后，你需要手动创建知识库：
1. 进入"知识库"视图
2. 点击"创建知识库"按钮
3. 填写知识库信息（名称、描述等）
4. 创建后即可开始使用

## 📁 项目结构

```
knowledgeOS/
├── frontend/              # 前端代码
│   ├── index.html         # 主页面
│   ├── css/               # 样式文件
│   ├── js/                # JavaScript 模块
│   │   ├── api.js         # API 封装
│   │   ├── ui.js          # UI 组件
│   │   ├── knowledge-items.js      # 知识点管理
│   │   ├── knowledge-extraction.js # 知识提取
│   │   ├── consultation.js         # 智能问答工作台
│   │   └── ...
│   └── assets/            # 静态资源
├── backend/               # 后端代码
│   ├── server.js          # Express 服务器入口
│   ├── routes/            # 路由模块
│   │   ├── items.js       # 文档管理
│   │   ├── knowledge.js   # 知识点管理
│   │   ├── ai.js          # AI 功能
│   │   ├── consultation.js # 智能问答服务
│   │   └── ...
│   ├── services/          # 业务逻辑
│   │   ├── ai.js          # AI 服务
│   │   ├── knowledge-extractor.js # 知识提取
│   │   ├── parser.js      # URL 解析
│   │   └── ...
│   ├── models/            # 数据模型
│   └── scripts/           # 脚本工具
├── database/              # SQLite 数据库文件（开发环境）
├── Dockerfile             # Docker 构建文件
├── package.json           # 项目依赖和脚本
└── README.md              # 项目说明
```

## 📡 API 端点

### 文档管理
- `GET /api/items` - 获取文档列表
- `GET /api/items/:id` - 获取单个文档
- `POST /api/items` - 创建文档
- `PUT /api/items/:id` - 更新文档
- `DELETE /api/items/:id` - 删除文档

### 知识点管理
- `GET /api/knowledge/items` - 获取知识点列表
- `GET /api/knowledge/items/:id` - 获取单个知识点
- `POST /api/knowledge/items` - 创建知识点
- `PUT /api/knowledge/items/:id` - 更新知识点
- `DELETE /api/knowledge/items/:id` - 删除知识点
- `POST /api/knowledge/extract` - 从文档提取知识
- `GET /api/knowledge/graph` - 获取知识图谱

### AI 功能
- `POST /api/ai/summary` - 生成摘要
- `POST /api/ai/chat` - AI 对话（流式响应）
- `POST /api/ai/suggest-tags` - 标签建议

### 知识库管理
- `GET /api/knowledge-bases` - 获取知识库列表
- `POST /api/knowledge-bases` - 创建知识库
- `PUT /api/knowledge-bases/:id` - 更新知识库
- `DELETE /api/knowledge-bases/:id` - 删除知识库

### 智能问答服务
- `POST /api/consultation/chat` - 智能问答对话
- `POST /api/consultation/analyze-document` - 分析文档

### 其他
- `POST /api/parse/url` - 解析 URL 并提取文本
- `POST /api/upload` - 文件上传
- `GET /api/tags` - 获取标签列表
- `GET /api/export` - 导出数据
- `GET /api/health` - 健康检查

完整的 API 文档请参考代码中的路由文件。

## 🚢 部署指南

### Docker 部署

项目包含 `Dockerfile`，支持 Docker 部署：

```bash
docker build -t knowledgeos .
docker run -p 3000:3000 -e DATABASE_URL=your_database_url knowledgeos
```

### Railway 部署（推荐）

详细部署指南请参考 [DEPLOY.md](DEPLOY.md) 和 [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)。

快速步骤：
1. 在 Railway 创建项目
2. 连接 GitHub 仓库
3. 添加 PostgreSQL 数据库
4. 配置 Volume（用于文件存储）
5. 自动部署

### 环境变量

#### 数据库配置
- `DATABASE_URL` - PostgreSQL 连接字符串（生产环境）
- `DB_TYPE` - 数据库类型（`postgres` 或默认 SQLite）
- `DATABASE_PATH` - SQLite 数据库路径（仅 SQLite）

#### 应用配置
- `PORT` - 服务端口（默认：3000）
- `NODE_ENV` - 环境模式（`production` / `development`）
- `CORS_ORIGIN` - CORS 允许的源（默认：`*`）

#### 文件存储
- `UPLOADS_PATH` - 上传文件存储路径（默认：`./uploads`）

## 💡 使用说明

### 添加内容

1. **添加 URL**：在输入框中输入 URL，系统会自动提取网页内容
2. **添加文本**：直接输入文本内容
3. **上传 PDF**：点击上传按钮选择 PDF 文件
4. **创建笔记**：使用 Memo 功能快速记录想法

### 提取知识点

1. 选择要提取的文档
2. 选择目标知识库
3. 点击"提取知识"按钮
4. 系统会使用 AI 自动提取知识点，支持实时查看进度

### 使用 AI 对话

1. 选择知识库和上下文
2. 在对话框中输入问题
3. AI 会基于知识库内容回答，并引用相关知识点

## 🤝 贡献

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

## 🔗 相关链接

- [DeepSeek Platform](https://platform.deepseek.com) - AI 服务提供商
- [Railway](https://railway.app) - 推荐的部署平台
- 问题反馈：[GitHub Issues](https://github.com/siegzhong-maker/knowledgeOS/issues)

## 🙏 致谢

- 感谢 [DeepSeek](https://www.deepseek.com/) 提供的 AI 能力
- 感谢所有贡献者的支持

---

如果这个项目对你有帮助，请给一个 ⭐ Star！
